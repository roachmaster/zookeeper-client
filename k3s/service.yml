apiVersion: v1
kind: Service
metadata:
  name: zookeeper-client
  labels:
    app: zookeeper-client
spec:
  ports:
    - port: 8080
      protocol: TCP
      name: zookeeper-client
  selector:
    app: zookeeper-client